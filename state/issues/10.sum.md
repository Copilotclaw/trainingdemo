

```markdown
### 2026-03-12

The user tested the automated summary feature by creating a new issue (#10) and commenting about the summary functionality. The agent confirmed that the system automatically generates both full thread logs (`full.md`) and running AI summaries (`sum.md`) for each issue. For new issues like #10, only the full log exists initially - the summary file gets created starting from the second message onwards. The agent explained that subsequent comments in an issue will include the AI-generated summary at the top to maintain context continuity. This demonstrates the workflow where cold starts use only the full thread while warm starts leverage the maintained summary for better context awareness.

```